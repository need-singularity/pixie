{
  "name": "pixie-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Pixie ✨ — Discord slash-command handler on Cloudflare Workers",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "register": "node scripts/register.mjs",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  },
  "dependencies": {
    "discord-interactions": "^4.1.0"
  }
}
