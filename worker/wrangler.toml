name = "pixie"
main = "src/index.js"
compatibility_date = "2026-04-01"
account_id = "d4acc95862b4203c11948da5baf079bc"

# Secrets (set via `wrangler secret put`, not committed):
#   DISCORD_PUBLIC_KEY     — Ed25519 verification
#   DISCORD_APPLICATION_ID — public, fine in plain vars
#   DISCORD_BOT_TOKEN      — reserved for future pin / edit features
